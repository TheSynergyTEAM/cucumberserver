spring:
  profiles:
    include: ouath,real-db
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
          storage-engine:innodb

  session:
    store-type: jdbc